package ejercicio3;

import java.util.Comparator;

public class Catalogador {

    /*
     * public Catalogo ontenerCatalogo(SitioWeb[] s, filtro f, String param) {
     * Catalogo c = new Catalogo();
     * switch (f) {
     * case INCLUYE:
     * for (SitioWeb sitioAux : s) {
     * for (String eti : sitioAux.getEtiquetas()) {
     * if (eti.equals(param)) {
     * c.getSitios().add(sitioAux);
     * }
     * }
     * }
     * break;
     * case AL_MENOS:
     * int n = Integer.parseInt(param);
     * for (SitioWeb sitioAux : s) {
     * if (sitioAux.getEtiquetas().size() >= n) {
     * c.getSitios().add(sitioAux);
     * }
     * }
     * break;
     * case CANT_VISITAS:
     * for (SitioWeb sitioAux : s) {
     * int num = Integer.parseInt(param);
     * if (sitioAux.getContador() > num) {
     * c.getSitios().add(sitioAux);
     * }
     * }
     * break;
     * }
     * return c;
     * }
     */
}